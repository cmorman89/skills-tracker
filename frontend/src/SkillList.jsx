import SkillTree from "./SkillTree";

const SkillList = () => {

    const items = ["one", "two", "three"]
    return(
        <SkillTree
            items={items}
        />
    );
}

export default SkillList;